# Changelog

本文件记录 review-quote-sh 的所有重要变更。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.2.0] — 2026-09-12

### 新增

- **DSH 原生设置页**：注册 `settings.section` 顶级设置 tab「🔍 审查与引用」，与「通用设置 / 模型 / 插件」同级；配置由 DSH Settings 系统持久化（`ctx.settings.register('review-quote-sh', schema)`）。
- **可配置多轮审查**：设置审查轮数（1~5）并为每一轮指定独立模型；审查时按配置并行调度，完成后自动汇总。
- **重答对比模式（方案 A）**：审查非代码回答时，审查模型先根据原始问题独立作答，再与待审查回答逐点对比，输出：
  1. 事实差异（孰对孰错，无法确认的标注「⚠️ 待核实」）
  2. 逻辑差异（论证结构与推理链条）
  3. 遗漏项
  4. 过度断言
  5. 表达质量
  可在设置中关闭，关闭后回退为纯五维审查。
- **思考模式可调**：每轮审查模型与总结模型均可单独选择思考强度（非思考 / 轻思考 / 高思考 / 极限思考），经请求级 `reasoningEffort` 传递；默认非思考。
- **弹窗内临时总结模型**：审查报告完成后，可在弹窗底部临时切换本次汇总所用模型（优先级高于设置中的默认值）。

### 修复

- 修复「选择 N 轮审查但只执行 1 轮」的问题：当第 N 轮模型未配置时，原逻辑静默过滤空值导致少审；现改为严格校验并明确提示缺失轮次（设置页同时显示红色警告）。

### 优化

- 设置导航图标与 DSH 设置面板同风格（线条放大镜，替代默认齿轮与彩色 emoji）。
- 提示词增强按钮的 ✨ emoji 替换为同风格线条图标（通过 CSS mask 覆盖，不修改第三方插件源码）。

### 兼容性

- 适配 DSH `0.1.5-rc.2`：核对 `settings.section`、`settingsScope`、`settings.plugin.item`、`reasoningEffort` 均未变更。
- `dsh.client.inject` 修正为「@deepseek-ai 包名数组」语义（本插件无额外客户端包依赖，置空）。
- 依赖仅保留 `react`（移除 `@deepseek-ai/schemastery` 声明——该包由 DSH 运行时提供，Host 端以动态 import + 回退处理）。

## [1.1.0] — 2026-08-22

### 新增

- 审查报告汇总：多模型互审后一键生成综合意见，标注模型间分歧点。
- 安全限流：Origin 白名单、请求体 2MB 上限、provider/model 白名单、30 次/分钟速率限制、最多 16 个并发审查任务。
- 审查耗时显示（实时刷新已用秒数）。

### 变更

- 偏好持久化迁移到 Host 端（`~/.dsh/.review-quote-prefs.json`），页面刷新不丢失。

## [1.0.1] — 2026-08-17

### 安全

- Origin 守卫（拒绝跨站与外部 Origin）。
- 请求体上限 2MB。
- provider/model 白名单校验。
- 速率与并发限制。
- 新增 summary 路由。

## [1.0.0] — 2026-08-15

### 新增

- 初始版本：多模型交叉审查（代码审查 + 回复审查）、引用问答胶囊、偏好记忆。
- 支持长内容先压缩再审查。
