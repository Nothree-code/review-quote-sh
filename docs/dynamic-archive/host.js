// review-quote-sh (消息审查与引用插件) — 正式版 v1.1 (pkg-17)// Host 半：审查任务执行器 + 模型目录枚举 + 偏好持久化(RPC) + 包内 RPC// 重新部署：将本文件内容作为 cordis_define 的 code.host 传入（与 client.js 配套）return {  inject: ['llm', 'timer'],  apply(ctx) {    const FACTS_DISCIPLINE = '\n\n事实核实纪律（重要）：审查中凡涉及外部世界事实的断言（模型名、产品名、API、版本号、URL、数据、事件），若你无法从自身知识确认其真实性，必须标注「⚠️ 待核实」并说明核实途径（如官方 API 模型列表、官方文档），严禁断言其不存在或虚构。你的训练知识存在截止日期，对近期发布的产品与模型名称尤其容易过时。请严格区分两类问题：\n- 文本内部矛盾/逻辑/结构错误：你可直接判定。\n- 外部世界事实：你只能标注「⚠️ 待核实」，不能作为错误判定。'    const CODE_PROMPT = '你是一位严格的资深代码审查专家。审查下面提供的代码片段，输出 Markdown 格式审查报告（中文）：\n1. 按严重程度分级列出问题：CRITICAL（正确性/安全/资源泄漏）、MAJOR（并发/性能/健壮性）、MINOR（风格/可维护性）、NIT（琐碎）。\n2. 每个问题给出：定位（行号或代码片段）、问题解释、具体修复建议（含修改后的代码）。\n3. 额外核查：可变默认参数、资源句柄管理、线程安全、浮点精度、异常处理路径、输入校验。\n4. 最后给出总体评价（3-5 句）和修复优先级排序列表。\n只依据提供的代码本身，不要臆测外部上下文。' + FACTS_DISCIPLINE    const REPLY_PROMPT = '你是一位严谨的回复审查专家。审查下面提供的 AI 回复文本，输出 Markdown 格式审查报告（中文）：\n1. 事实准确性：有无事实错误、夸大或未经证实的断言。\n2. 逻辑与一致性：论证是否自洽，有无前后矛盾、答非所问。\n3. 完整性：是否遗漏了问题的关键方面。\n4. 可验证性：关键结论是否有依据或可核查的来源线索。\n5. 表达质量：结构、冗余、清晰度。\n每项给出具体引用片段和修改建议，最后给出总体评价（3-5 句）和优先改进项。只依据提供的文本本身。' + FACTS_DISCIPLINE    const COMPRESS_PROMPT = '你是对话压缩助手。把下面提供的对话/代码内容压缩成一份紧凑但信息完整的摘要，供后续代码审查或回复审查使用：\n1. 保留所有关键代码片段、问题描述、上下文线索、主要结论和待办事项。\n2. 去除寒暄、重复和无关细节。\n3. 使用简洁的中文描述，代码保持原文。\n4. 输出纯文本，尽量控制在 15000 字以内。\n5. 如果内容是多轮对话，请用「[轮次N] 用户：… 助手：…」的格式保留轮次结构，让审查者能分辨上下文层次。'    const CODE_HINTS = {      QUOTA: '余额不足（QUOTA）：请到模型服务商平台充值后重试',      RATE_LIMIT: '触发限流（429）：请稍候重试',      CONTEXT_WINDOW_EXCEEDED: '内容超过模型上下文上限：请缩小审查范围，或换用更大上下文的模型（如 kimi-k3）',      AUTH: 'API Key 无效或过期：请在设置中更新凭据',      TIMEOUT: '请求超时：请重试',      TRANSPORT: '网络连接失败：请检查网络后重试',      EMPTY_RESPONSE: '模型返回为空：请重试',    }    const hintFor = (f) => {      if (f && f.code && CODE_HINTS[f.code]) {        const extra = f.message ? '（' + String(f.message).slice(0, 200) + '）' : ''        return CODE_HINTS[f.code] + extra      }      return null    }    const streamText = async (provider, model, system, text, maxTokens) => {      let out = ''      for await (const chunk of ctx.llm.stream({        provider,        model,        system,        messages: [{ role: 'user', content: [{ type: 'text', text }] }],        maxTokens,      })) {        if (chunk.type === 'text-delta') out += chunk.text        else if (chunk.type === 'finish' && chunk.reason.kind === 'error') {          const f = chunk.reason.failure          return { ok: false, error: hintFor(f) || ((f && f.message) || '模型调用失败') }        }      }      if (!out.trim()) return { ok: false, error: '模型未返回内容' }      return { ok: true, text: out }    }    const compressText = async (text) => {      const res = await streamText('deepseek-official', 'deepseek-v4-flash', COMPRESS_PROMPT, text, 8000)      if (!res.ok) {        const head = text.slice(0, 25000)        const tail = text.slice(-5000)        return { ok: true, text: head + '\n…[中间内容省略，共 ' + text.length + ' 字符]…\n' + tail, truncated: true }      }      return res    }    // Preferences persist on the Host so page refreshes keep them (process restart resets).    let savedPrefs = null    const jobs = new Map()    let jobSeq = 0    const runReview = async (jobId, job, provider, model, content) => {      let finalContent = content      let compressed = false      if (content.length > 30000) {        const comp = await compressText(content)        if (!comp.ok) {          job.error = '内容过长（' + content.length + ' 字符）且压缩失败：' + comp.error          job.done = true          return        }        finalContent = comp.text        compressed = true      }      const hasCode = /```[\s\S]*```/.test(finalContent) || /\b(function|def|class|import|const|let|return|public|private|static|#include|interface|type)\b/.test(finalContent)      const system = (compressed ? '注意：下面提供的内容可能是一份压缩后的对话摘要，请基于它审查。\n\n' : '') + (hasCode ? CODE_PROMPT : REPLY_PROMPT)      try {        for await (const chunk of ctx.llm.stream({          provider,          model,          system,          messages: [{ role: 'user', content: [{ type: 'text', text: finalContent }] }],          maxTokens: 12000,        })) {          if (job.cancelled) break          if (chunk.type === 'text-delta') job.text += chunk.text          else if (chunk.type === 'usage') {            job.usage = { input: chunk.usage.inputTokens, output: chunk.usage.outputTokens }          } else if (chunk.type === 'finish' && chunk.reason.kind === 'error') {            const f = chunk.reason.failure            job.error = hintFor(f) || ((f && f.message) || '模型调用失败')            break          }        }        if (!job.error && !job.text.trim()) job.error = '模型未返回内容'      } catch (e) {        job.error = (e && e.message) ? e.message : String(e)      } finally {        job.done = true        ctx.timeout(() => jobs.delete(jobId), 120000)      }    }    harness.handle('prefs-get', () => ({ prefs: savedPrefs }))    harness.handle('prefs-set', (args) => {      if (args && args.prefs) savedPrefs = args.prefs      return { ok: true }    })    harness.handle('review-options', async () => {      const out = []      const seen = new Set()      // Enumerate every provider the user configured under llm-pi-ai settings,      // plus DeepSeek official; the configured Kimi models are already there.      const providers = []      const settings = ctx.get('settings')      if (settings) {        try {          const s = settings.get('llm-pi-ai')          if (s && s.providers && typeof s.providers === 'object') {            providers.push.apply(providers, Object.keys(s.providers))          }        } catch (e) {}      }      if (!providers.length) providers.push('moonshotai-cn')      providers.push('deepseek-official')      for (const provider of providers) {        if (seen.has(provider)) continue        seen.add(provider)        try {          const models = await ctx.llm.listModels(provider)          for (const m of models) out.push({ provider, model: m.id, name: m.name || m.id })        } catch (e) {}      }      return out    })    harness.handle('review-start', async (args) => {      const provider = args && args.provider      const model = args && args.model      const content = args && args.content      if (!provider || !model || typeof content !== 'string' || !content.trim()) {        return { ok: false, error: '缺少 provider/model/content 参数' }      }      const jobId = 'rv-' + (++jobSeq)      const job = { text: '', done: false, error: null, cancelled: false, usage: null }      jobs.set(jobId, job)      runReview(jobId, job, provider, model, content)      return { ok: true, jobId }    })    harness.handle('review-poll', (args) => {      const job = jobs.get(args && args.jobId)      if (!job) return { done: true, error: '审查任务不存在或已过期' }      return { done: job.done, cancelled: job.cancelled, error: job.error, text: job.text, usage: job.usage }    })    harness.handle('review-cancel', (args) => {      const job = jobs.get(args && args.jobId)      if (job) job.cancelled = true      return { ok: true }    })  },}